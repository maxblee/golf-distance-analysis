packrat::init(".")
packrat::status()
# Setting up
# Setting up
packrat::status()
packrat::status()
x <- packrat::status()
x
survey_data <- read_tsv("survey.tsv") %>%
# convert vote column to boolean so we can use it with a logistic regression model
mutate(cand_a = vote == "A")
model <- glm(cand_a ~ 1 + state + sex + age + education + ideology, data=survey_data, family="binomial")
summary(model)
survey_data <- read_tsv("survey.tsv") %>%
# convert vote column to boolean so we can use it with a logistic regression model
mutate(cand_a = vote == "A")
model <- glm(cand_a ~ 1 + state + sex + age + education + ideology, data=survey_data, family="binomial")
summary(model)
survey_data <- read_tsv("survey.tsv") %>%
# convert vote column to boolean so we can use it with a logistic regression model
mutate(cand_a = vote == "A")
library(tidyverse)
setwd("~/Documents/stanford-journalism/winter_quarter/stats/assignments/assignment_six")
survey_data <- read_tsv("survey.tsv") %>%
# convert vote column to boolean so we can use it with a logistic regression model
mutate(cand_a = vote == "A")
model <- glm(cand_a ~ 1 + state + sex + age + education + ideology, data=survey_data, family="binomial")
summary(model)
library(tidyverse)
library(tidyverse)
library(tidyverse)
exit
quit()
library(tidyverse)
install.packages(tidyverse)
install.packages("tidyverse")
